# Automation Overview

The automation runs once per day and orchestrates multiple PowerShell scripts.

Each script performs a specific operational check and outputs results to a report file.

Only exceptions require manual review.
